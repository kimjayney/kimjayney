#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
using namespace std;
int main() {
    cin.tie(NULL);
    std::ios_base::sync_with_stdio(false);
    
    char s;
    cin >> s;
    std::cout << static_cast<int>(s) << std::endl;
     
    return 0;
}